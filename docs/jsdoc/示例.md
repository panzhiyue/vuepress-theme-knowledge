```javascript
/**
 * @typedef {enum} LineCap 线条的结束端点样式
 * @property {string} LineCap.BUTT  向线条的每个末端添加平直的边缘。
 * @property {string} LineCap.ROUND  向线条的每个末端添加圆形线帽。
 * @property {string} LineCap.SQUARE  向线条的每个末端添加正方形线帽。
 */
```