





circle

```javascript
            image: new Circle({
              radius: 2,
              fill: new Fill({
                color: "blue",
              }),
              stroke: new Stroke({
                color: "white",
                width: 1,
              }),
            }),
```

icon
```javascript
        image: new style.Icon({
          anchor: [0.5, 1],
          scale: 0.15,
          src: markerIcon,
        }),
```

text

```
              text: new Text({
                text: "图斑",
                overflow: true,
                fill: new Fill({
                  color: "#ffffff",
                }),
                font: "20px sans-serif",
              }),
```

