circle

```javascript
            image: new Circle({
              radius: 2,
              fill: new Fill({
                color: "blue",
              }),
              stroke: new Stroke({
                color: "white",
                width: 1,
              }),
            }),
```

icon
```javascript
        image: new style.Icon({
          anchor: [0.5, 1],
          scale: 0.15,
          src: markerIcon,
        }),
```

fill
```javascript
        fill: new style.Fill({
          color: "rgba(255,255,255,0.4)",
        }),
```

stroke
```javascript
        stroke: new style.Stroke({
          color: "#3399CC",
          width: 1.25,
        }),
```

text

```
              text: new Text({
                text: "图斑",
                overflow: true,
                fill: new Fill({
                  color: "#ffffff",
                }),
                font: "20px sans-serif",
              }),
```

